name            = "ember-ai-proxy"
main            = "worker.js"
compatibility_date = "2024-09-01"

# Set the API key with:  wrangler secret put DEEPSEEK_API_KEY
# It is stored encrypted by Cloudflare — never appears in this file or Git.
